@model IEnumerable<TourPal1._0.Models.Activity>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>

    @if (User.IsInRole("Administrator"))
    {
        @Html.ActionLink("Create New", "Create")
    }
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Destination)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TimeToGo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BriefDescription)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MaximumNumberOfParticipant)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Administrator.FirstName)
            @Html.Label("(Administrator)")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Destination)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TimeToGo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BriefDescription)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MaximumNumberOfParticipant)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Administrator.FirstName)
        </td>
        <td>
            @if (User.IsInRole("Administrator"))
            {
                @Html.ActionLink("Edit", "Edit", new { id = item.Id })

            }
            @if (User.Identity.Name == "htyrone9523@gmail.com")
            {
                @Html.ActionLink("Delete", "Delete", new { id = item.Id })
            }
             @Html.ActionLink("Details", "Details", new { id = item.Id })
        </td>
    </tr>
}

</table>
